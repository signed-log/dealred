{
  "name": "dealred",
  "description": "Redirects from the annoying Dealabs tracker",
  "engines": {
    "node": ">=14.0.0",
    "npm": ">=6.14.0"
  },
  "scripts": {
    "build": "web-ext build --overwrite-dest --source-dir ./src/",
    "run-android": "web-ext run -t firefox-android --source-dir ./src/",
    "test": "web-ext lint --source-dir ./src/ || true"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/signed-log/dealred.git"
  },
  "author": "signed-",
  "license": "Apache-2.0",
  "bugs": {
    "url": "https://github.com/signed-log/dealred/issues"
  },
  "homepage": "https://github.com/signed-log/dealred",
  "devDependencies": {
    "eslint": "^8.29.0",
    "eslint-config-airbnb-base": "^15.0.0",
    "eslint-plugin-import": "^2.26.0",
    "eslint-plugin-n": "^15.6.0",
    "eslint-plugin-no-unsanitized": "^4.0.2",
    "eslint-plugin-promise": "^6.1.1",
    "prettier": "^2.8.1",
    "web-ext": "^7.4.0",
    "webextension-polyfill": "^0.10.0"
  }
}
